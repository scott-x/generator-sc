import React, { PureComponent } from 'react';
import {
	HeaderWrapper
} from './style';

class Header extends PureComponent {
  render() {
    return (
     <HeaderWrapper>
        I am Header
     </HeaderWrapper>
    );
  }
}

export default Header;